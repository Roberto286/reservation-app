{
  "packageManager": "pnpm@8.14.1",
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "scripts": {
    "start:dev": "turbo run start:dev",
    "start:docker": "pnpm --filter @reservation-app/shared build --watch & docker compose up --build -d"
  }
}

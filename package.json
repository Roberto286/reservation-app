{
  "packageManager": "pnpm@8.14.1",
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "scripts": {
    "start:dev": "turbo run start:dev",
    "start:docker": "docker compose up --build -d",
    "start:docker-prod": "docker compose -f compose.prod.yml up --build -d"
  }
}
